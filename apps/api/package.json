{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "tsc && node ./dist/index.js || exit 1",
    "dev": "nodemon -e ts",
    "start": "node dist/index.js",
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.100.0",
    "@aws-sdk/s3-request-presigner": "^3.100.0",
    "cors": "^2.8.5",
    "express": "4.18.1",
    "nodemon": "^2.0.16"
  },
  "devDependencies": {
    "@types/express": "4.17.13",
    "kill-port": "^2.0.0",
    "tsup": "6.0.1",
    "typescript": "4.5.5"
  }
}
